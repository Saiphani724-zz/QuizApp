Update quizInfo in DB
Show user Customised quizes
ON end test change is Available to False



register
	Front end Validation

Settings page
Comments Section

Change DB structure 
UserDB - 
	Link Quiz Details to user - toAnswer / isAnswered / running(time stored in app state)
	Add quiz, answered questions

QuizDB - 
	quizCode,
	timer,
	explanation for question



Student Dashboard
	TestArena
		Submit
		Timer
	View Result
	Ques, show/hide Result
.

.
.
.
Faculty Dashboard

ToApp
[Adjust all warnings and comment log statements
Embbedd username and sign up in cards
Get good color values
adjust margins and for each page try full and half window display]


Selective conducting of quizes
JWT token authorisation


// 	var reqQuizes = {}
			// 	// req.query.prvQuiz
			// 	db.collection('users').findOne(
			// 		{
			// 			'username': uname.toLowerCase(),
			// 		}
			// 		, (err, user) => {
			// 			if (err) {
			// 				console.log("Error is", err);
			// 				return;
			// 			}
			// 			for(let i = 0;i < user.quizInfo.length;i++){
			// 				var quiz = user.quizInfo[i];
			// 				if(quiz.prvQuiz === req.query.prvQuiz){
			// 					reqQuizes[quiz['quizCode']] = 1;
			// 				}
			// 			}
			// 		})